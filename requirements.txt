# Görüntü işleme (önizleme penceresi için tam sürüm; sunucuda opencv-python-headless kullanılabilir)
opencv-python>=4.8
numpy>=1.24

# Barkod çözümleme (Linux'ta sistemde libzbar gerekir: apt install libzbar0)
pyzbar>=0.1.9

# Ekran yakalama (kameranın yansıdığı ekranı okumak için)
mss>=9.0

# Yapılandırma dosyası
PyYAML>=6.0
